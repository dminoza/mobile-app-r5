import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    inputField: {
        borderColor: 'black',
        width: 200,
        borderWidth: 2,
        marginBottom: 10,
        paddingStart: 10,
        paddingEnd: 10
    },
    label: {
        fontWeight: 'bolder',
        fontSize: 14
    }
})